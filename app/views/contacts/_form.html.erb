<script type="text/javascript">
jQuery.noConflict();
 jQuery(document).ready( function() {
//     jQuery("#new_contact").validate({
// 	rules: {
// 	    entry: {
// 		required: function(element) {
// 		    alert("Element " );
// 		    return false;
// 		}
		
// 	    }
// 	},
// 	messages: {
// 	    contact_entry: "Please enter a valid email address"
// 	}
//     });
    
    jQuery("#entryLabel").text("Please enter a valid " + jQuery("#contact_contact_type_id :selected").text());
    jQuery("#contact_contact_type_id").change( function () {
        selectedText = jQuery('#contact_contact_type_id :selected').text() ;
	jQuery("#entryLabel").text("Please enter a valid " + selectedText);
    });
});
</script>
<div id="addTypes" >
<div class="resetLayout" >
  <%= form_for @contact, :validate => true do |f| %>
  <%= f.error_messages %>
    <%= f.hidden_field :user_id %>
    <% unless @group.nil? %>
       <%= f.fields_for @group do |fd| %>
       <%= fd.hidden_field :id %>
       <% end %>
    <% end %>
  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
<p>
  Preferred method of contact
  <%= f.select "type", options_for_select(Contact.select_options) %>
</p>
 <p>
    <label id="entryLabel" for="entry">Entry Number or email address</label><br />
    <%= f.text_field :entry %>
  </p>
  <p><%= f.submit %></p>
<% end %>
</div>
</div>
